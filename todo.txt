1、为每个任务或流程（包括子流程）设计各种状态标记，包括未启动("NotStarts")、进行中("InProgress")、完成("completed")、取消("Cancelled")、失败("Failed")、超时("Timeout")
[已完成] 创建了workflow_status.py模块，定义了WorkflowStatus、AnalysisTaskStatus和PriceCalculationStatus枚举

2、投标文件AI大模型规则打分子流程启动触发条件错误：
目前是如下：

触发点：在AnalysisManager.analysis_task方法中
触发条件：
项目ID、投标文件ID、招标文件路径和投标文件路径都必须提供
投标文件记录必须存在于数据库中
投标文件状态更新为"processing"
创建IntelligentBidAnalyzer实例并调用analyze_bidding_document方法
执行流程：
从数据库加载评分规则
提取投标文件内容（优先从MD文件读取）
确保投标人名称已正确设置
执行AI分析（串行处理规则分析）
更新分析结果到数据库


正确的是：触发条件：
价格分数计算子流程状态标记为"completed"
投标文件状态更新为"processing"
项目ID、投标文件ID、招标文件路径和投标文件路径都必须提供
投标文件记录必须存在于数据库中


[已完成] 在AnalysisManager.analysis_task方法中添加了触发条件检查，确保满足状态标记的统一标准

3、价格计算子流程启动触发条件错误：
目前是：
触发点：在AnalysisManager.run_analysis_and_calculate_prices方法中
触发条件：
项目ID和投标文件信息列表必须提供
评分规则初始化成功
项目信息存在且包含招标文件路径
所有投标文件分析任务完成
创建PriceCalculationWorkflow实例并调用execute_workflow方法
执行流程：
提取所有投标人的信息（名称和价格）
创建投标人价格数组
获取价格评分规则
构造发送给AI大模型的prompt
调用AI大模型计算价格分数
将价格分数保存到数据库
更新项目状态

正确的是：

触发条件：
招标文件解析完成(任务状态标记为"completed")
价格计算子流程状态标记为"processing"
项目ID和投标文件信息列表必须提供
评分规则初始化成功
项目信息存在且包含招标文件路径
所有投标文件分析任务尚未开始
创建PriceCalculationWorkflow实例并调用execute_workflow方法
执行流程：
提取所有投标人的信息（名称和价格）
创建投标人价格数组
获取价格评分规则
构造发送给AI大模型的prompt
调用AI大模型计算价格分数
将价格分数保存到数据库
更新项目状态

[已完成] 在AnalysisManager.run_analysis_and_calculate_prices方法和PriceCalculationWorkflow.execute_workflow方法中添加了触发条件检查

4、价格规则提取和处理问题：
目前是：
价格最高分值使用默认值40分，而不是从招标文件中提取

正确的是：
从招标文件中正确提取价格评分规则，包括最高分值
正确处理数据库中的价格规则，优先使用Parent_max_score字段
不再使用懒惰的默认值40分

[已完成] 创建了extract_scoring_rules.py测试脚本验证价格规则提取
[已完成] 修复了CorrectScoringExtractor模块，确保价格规则正确提取
[已完成] 更新了price_calculation_workflow.py文件，正确处理价格最高分值
[已完成] 创建了测试验证数据库中的评分规则处理
[已完成] 成功从招标文件中提取评分规则，包括价格规则的最高分值（40.0分）

5、评分规则父项标记错误问题：
目前是：
父项标记错误，即使有子项也标记为非父项

正确的是：
正确识别父项，有子项的规则应标记为父项

[已完成] 修复了CorrectScoringExtractor模块中的父项识别逻辑
[已完成] 更新了_build_hierarchy方法，确保正确标记父项和子项
[已完成] 验证修复后的评分规则提取，所有父项正确标记

6、汇总表显示问题：
目前是：
表头第二行出现空白子项
子项得分显示为空白

正确的是：
表头第二行应只显示子项名称，不应包含父项的空白子项
子项得分应正确显示分析结果

[已完成] 发现并修复了analysis_manager.py中保存分析结果时detailed_scores格式错误的问题
[已完成] 将detailed_scores从字典格式改为列表格式保存到数据库

7、规则打分流程未启动问题：
目前是：
规则打分流程未启动，导致子项得分为空白

正确的是：
规则打分流程应在正确条件下启动，满足状态标记的统一标准

[已完成] 修正了AnalysisManager.analysis_task方法中的触发条件检查，确保满足状态标记的统一标准
[已完成] 修复了PriceCalculationWorkflow中项目状态更新逻辑，正确更新为"completed"
[已完成] 修正了AnalysisManager.run_analysis_and_calculate_prices方法中的流程执行顺序
[已完成] 在file_controller.py中正确初始化投标文件状态
[已完成] 在价格计算完成后正确更新投标文件状态并启动规则打分流程

8、汇总表显示时机错误问题：
目前是：
在规则打分还没结束前，滚动页面就会触发汇总表显示，但这时候数据都不全

正确的是：
只有在所有分析任务（包括价格计算和规则打分）都完成后再触发汇总表显示

[已完成] 修正了PriceCalculationWorkflow中项目状态更新逻辑，避免过早更新项目状态
[已完成] 在AnalysisManager中添加了检查所有分析任务完成状态的方法
[已完成] 确保只有在所有分析任务都完成后再更新项目状态为"completed"
[已完成] 修正了前端轮询机制，确保在正确时机触发汇总表显示

9、流程状态错误问题：
目前是：
分析任务触发条件检查过于严格，导致在"analyzing"状态下无法启动分析任务

正确的是：
分析任务应在价格计算完成后正确启动，投标文件状态更新为"processing"后启动规则打分

[已完成] 修正了AnalysisManager.analysis_task方法中的触发条件检查，允许在"analyzing"状态下启动分析任务
[已完成] 确保在分析任务完成后正确更新投标文件状态为"completed"
[已完成] 修正了项目状态更新逻辑，确保在所有分析任务完成后更新项目状态
[已完成] 修复了AnalysisManager.run_analysis_and_calculate_prices方法中的流程执行顺序，确保价格计算工作流正确启动
