# AI_env2 项目完整文档

## 1. 项目概述

### 1.1 项目背景
AI_env2 是一个基于人工智能的投标文件评估系统，旨在通过自动化分析和评分提高评标效率和准确性。

### 1.2 目标用户
- 招标代理机构
- 政府采购部门
- 企业采购部门
- 需要处理大量投标文件的组织

### 1.3 核心问题
1. 人工评标效率低、耗时长
2. 评分规则提取和应用容易出错
3. 投标文件内容复杂，需要OCR和语义理解能力
4. 价格评分规则复杂，需要智能解析和计算

## 2. 系统功能

### 2.1 主要功能
1. 上传招标文件和投标文件（PDF格式）
2. 自动从招标文件中提取评分规则并保存到数据库
3. 使用本地AI模型（Ollama + qwen3）进行智能分析和评分
4. 展示评估结果汇总表
5. 提供详细的数据可视化分析（图表、可切换视图的详细表格）
6. 支持历史记录查看
7. 智能价格分计算

### 2.2 关键技术特性
- 支持中文OCR识别
- 使用本地AI模型保障数据隐私
- 支持价格分、技术分等多维度评分计算
- 智能解析复杂评分规则
- PDF文本缓存优化，避免重复OCR处理
- 清晰的多层表头结果展示与深入的数据分析功能

## 3. 技术架构

### 3.1 整体架构
基于 FastAPI 的前后端一体化架构，分层更清晰，并为路由拆分预留结构：

- **前端**：HTML/CSS/JavaScript + Jinja2 模板引擎 + Chart.js（仅在需要页面加载）
- **后端**：Python 3.10+ + FastAPI + Uvicorn
- **数据库**：SQLite（tender_evaluation.db）
- **AI模型**：Ollama 运行 qwen3:latest 模型
- **PDF处理**：paddleocr、paddlepaddle、PyMuPDF(fitz)、pdfplumber、PyPDF2、pikepdf
- **OCR**：Tesseract OCR（含中文语言包）

### 3.2 设计模式
- 工厂模式（用于模块初始化）
- 单例模式（数据库连接）
- 策略模式（评分规则提取与分析）
- 混入模式（功能模块化）

### 3.3 架构特点
- 分层架构（数据访问层、业务逻辑层、表示层）
- 模块化设计（评分规则提取、价格分计算、AI分析等独立模块）
- 向后兼容的模块化重构，单个文件不超过800行

## 4. 核心模块

### 4.1 数据库模块 (modules/database.py)
使用 SQLAlchemy ORM 实现数据库操作，主要数据表：

- **TenderProject**：招标项目信息
- **BidDocument**：投标文件信息
- **AnalysisResult**：分析结果
- **ScoringRule**：评分规则
- **ScoreModificationHistory**：评分修改历史
- **ProjectAuditLog**：项目审计日志

### 4.2 评分规则提取系统 (modules/scoring_extractor/)
模块化重构后的评分规则提取系统，负责从招标文件中解析出评分规则并存入数据库。

优化内容：
- 使用TableAnalyzer模块直接读取评分规则章节的表格信息来提取评分规则
- 删除多余代码，防止重复实现
- 优化评分规则名称和描述的清洗处理
- 改进重复规则检测机制，确保价格规则不会重复提取
- 优化max_score提取逻辑，直接从criteria_name的括号内提取分数

### 4.3 价格管理模块 (modules/price_manager.py)
- 价格信息提取和处理
- 价格分计算

### 4.4 智能投标分析模块 (modules/intelligent_bid_analyzer.py)
- 投标文件智能分析
- AI评分处理
- 价格提取和计算
- 进度跟踪和数据库更新

### 4.5 其他核心模块
- **PDF处理**：文本提取、并行页级提取、OCR处理、缓存机制优化（`modules/pdf_processor.py`）
- **结果展示**：动态汇总表（双层表头）生成（`modules/summary_generator.py`）
- **本地AI分析**：本地 AI 模型调用（`modules/local_ai_analyzer.py`）
- **投标方识别**：整合正则/AI/章节锚点/表格回退的统一流程（`modules/bidder_name_extractor.py`）

## 5. 系统完整流程

### 5.1 系统启动流程
```
启动FastAPI应用 (main.py)
├── 初始化数据库表
├── 创建上传目录
├── 配置静态文件和模板
└── 启动uvicorn服务器 (端口8000)
```

### 5.2 项目创建和文件上传流程
```
用户访问前端界面
├── 上传招标文件 (PDF)
│   ├── 保存到uploads目录
│   ├── 创建TenderProject记录
│   └── 自动调用initialize_project_analysis()
│
└── 上传投标文件 (PDF)
    ├── 保存到uploads目录
    ├── 创建BidDocument记录
    └── 等待分析启动
```

### 5.3 评分规则提取流程 (initialize_project_analysis)
```
initialize_project_analysis(project_id)
├── 获取项目信息
├── 检查招标文件是否存在
├── 使用IntelligentScoringExtractor提取评分规则
│   ├── 从招标文件PDF中提取文本
│   ├── 使用AI分析提取评分规则
│   └── 解析规则结构
├── 使用ScoringRulesManager保存规则到数据库
└── 返回提取结果
```

### 5.4 投标文件分析流程 (run_analysis_and_calculate_prices)
```
run_analysis_and_calculate_prices(project_id, bid_files_info)
├── 获取所有投标文件
├── 为每个投标文件创建分析任务
│   └── 调用analysis_task()进行单个文件分析
     └── 保存每个单项规则到数据库

价格分析单独进行：
├── 获取所有投标文件中的投标总价
├── 将全部投标方和其对应的投标总价、价格分析公式或描述，发送给AI大模型，获取每个投标方的价格分
└── 保存每个投标方的价格分到数据库
```

### 5.5 单个投标文件分析流程 (analysis_task)
```
analysis_task(project_id, bid_document_id, tender_file_path, bid_file_path)
├── 更新投标文件状态为"processing"
├── 创建IntelligentBidAnalyzer
│   ├── 初始化PDF处理器
│   ├── 提取投标文件文本 (PDFProcessor.process_pdf_full_text)
│   └── 获取评分规则 (从数据库查询)
│
├── 执行AI分析 (analyze_bidding_document)
│   ├── 构建AI分析prompt
│   ├── 调用LocalAIAnalyzer.analyze_text()
│   ├── 解析AI响应获取评分结果
│   └── 提取投标人名称和投标总价
│
├── 保存分析结果到数据库
│   ├── 更新BidDocument记录
│   └── 创建/更新AnalysisResult记录
│
└── 更新状态为"completed"
```

### 5.6 AI分析详细流程 (IntelligentBidAnalyzer)
```
analyze_bidding_document()
├── 文本提取阶段
│   ├── 使用PDFProcessor.process_pdf_full_text()
│   ├── 支持OCR处理 (PaddleOCR/RapidOCR)
│   └── 保存文本到临时文件
│
├── 评分规则获取
│   ├── 从数据库查询ScoringRule表
│   └── 构建评分规则文本
│
├── AI分析阶段
│   ├── 构建完整prompt (规则+投标文件文本)
│   ├── 调用LocalAIAnalyzer.analyze_text()
│   ├── 解析AI响应获取JSON格式结果
│   └── 提取投标人名称、投标总价、评分结果
│
└── 结果保存
    ├── 更新BidDocument.bidder_name
    ├── 更新BidDocument.total_price
    └── 保存详细评分结果到AnalysisResult
```

### 5.7 价格分数计算流程
```
PriceScoreCalculator.calculate_price_scores()
├── 获取所有投标文件的价格
├── 找到最低价格 (基准价)
├── 计算每个投标文件的价格分数
│   └── 公式: (基准价 / 投标价) × 价格分满分
└── 保存价格分数到数据库
```

### 5.8 前端展示流程
```
用户查看分析结果
├── 获取项目列表 (get_all_projects)
├── 获取分析状态 (get_analysis_status)
├── 获取分析结果 (get_analysis_results)
├── 获取评分规则 (get_scoring_rules)
└── 导出结果 (Excel/Word)
```

## 6. 核心模块关系图
```
main.py (FastAPI应用)
├── modules/analysis_manager.py (分析管理器)
│   ├── initialize_project_analysis() - 评分规则提取
│   └── run_analysis_and_calculate_prices() - 投标文件分析
│
├── modules/intelligent_bid_analyzer.py (智能投标分析器)
│   ├── analyze_bidding_document() - 完整AI分析
│   └── 集成PDF处理、AI调用、结果解析
│
├── modules/pdf_processor.py (PDF处理器)
│   ├── process_pdf_full_text() - 完整文本提取
│   └── 支持OCR处理
│
├── modules/scoring_extractor/ (评分规则提取器)
│   └── IntelligentScoringExtractor - 从招标文件提取规则
│
├── modules/price_score_calculator.py (价格分数计算器)
│   └── calculate_price_scores() - 计算价格分数
│
└── modules/database.py (数据库模型)
    ├── TenderProject - 项目信息
    ├── BidDocument - 投标文件
    ├── AnalysisResult - 分析结果
    └── ScoringRule - 评分规则
```

## 7. 数据流向图
```
招标文件PDF → IntelligentScoringExtractor → ScoringRule表
投标文件PDF → PDFProcessor → 文本内容
文本内容 + 评分规则 → LocalAIAnalyzer → 分析结果
分析结果 → AnalysisResult表
所有价格 → PriceScoreCalculator → 价格分数
```

## 8. 关键API端点
```
POST /api/projects - 创建项目
POST /api/projects/{project_id}/upload-tender - 上传招标文件
POST /api/projects/{project_id}/upload-bid - 上传投标文件
POST /api/projects/{project_id}/confirm-names-and-start-analysis - 确认名称并开始分析
GET /api/projects/{project_id}/analysis-status - 获取分析状态
GET /api/projects/{project_id}/results - 获取分析结果
GET /api/projects/{project_id}/scoring-rules - 获取评分规则
GET /api/projects/{project_id}/export-excel - 导出Excel结果
GET /api/projects/{project_id}/export-word - 导出Word结果
```

## 9. 数据库表关系
```
TenderProject (项目表)
├── id (主键)
├── project_name (项目名称)
├── tender_file_path (招标文件路径)
└── status (项目状态)

BidDocument (投标文件表)
├── id (主键)
├── project_id (外键 -> TenderProject.id)
├── file_path (投标文件路径)
├── bidder_name (投标人名称)
├── total_price (投标总价)
└── processing_status (处理状态)

AnalysisResult (分析结果表)
├── id (主键)
├── project_id (外键 -> TenderProject.id)
├── bid_document_id (外键 -> BidDocument.id)
├── bidder_name (投标人名称)
├── total_score (总分数)
├── price_score (价格分数)
└── detailed_scores (详细评分JSON)

ScoringRule (评分规则表)
├── id (主键)
├── project_id (外键 -> TenderProject.id)
├── Parent_Item_Name (父项名称)
├── Child_Item_Name (子项名称)
├── Child_max_score (子项满分)
├── is_price_criteria (是否价格标准)
└── description (规则描述)
```

## 10. 关键技术实现

### 10.1 评分规则提取优化
- **表格结构识别**：增强单元格内容合并逻辑。
- **分数识别**：添加有效性验证。
- **章节提取**：改进边界检测，确保只提取"评标办法"相关章节。
- **数据清洗**：增强评分规则名称和描述的清洗处理，统一数据格式。

### 10.2 价格评分规则处理
- **智能解析**：不使用正则表达式提取价格计算公式的方法，将从招标文件中提取的评分规则中的价格规则存储到数据库中。
- **公式存储**：数据库支持价格计算公式存储。
- **强约束**：在全部投标人的投标文件分析结束后，启动价格计算流程。

### 10.3 性能优化
- **PDF缓存**：使用文件哈希+元信息作为缓存标识，避免重复处理。
- **并发提取**：页级并发提取，带页超时与总超时控制。
- **价格提取**：价格一览表特殊处理；大小写金额对照验证。
- **表格识别优化**：增强表格结构识别逻辑，确保跨页表格正确合并。

### 10.4 结果展示优化
- **汇总表**：多层表头、内容可读性、水平滚动
- **详细分析视图**：价格与得分图表、详细评分表格

## 11. 数据处理流程

1. **初始化上传**：上传招标与投标文件，后端快速识别投标人名称并返回前端确认可编辑清单。
2. **确认名称并启动分析**：前端确认后，写回数据库并启动后台分析与价格分计算。
3. **结果呈现**：在前端生成汇总表。用户可点击"详细分析"按钮，查看价格对比图表和可切换视图的详细评分表。

## 12. 开发与部署

### 12.1 环境要求
- **开发**：Python 3.10+、Tesseract OCR（中文语言包）、Ollama（qwen3模型）
- **部署**：FastAPI应用（支持Gunicorn + Uvicorn），确保Ollama服务运行

### 12.2 搭建步骤
```
git clone <repository_url>
cd AI_env2
pip install -r requirements.txt
uvicorn main:app --reload  # 或 python main.py
```

## 13. 技术约束与规范

### 13.1 代码规范
- 遵循 Python 标准编码规范，模块化设计。
- 保持代码整洁，单个文件不超过800行。

### 13.2 性能与安全
- AI分析耗时取决于硬件性能，使用本地模型保障数据隐私。
- 文件上传需要验证格式与大小。

### 13.3 数据结构与处理规范
- 评分规则父子项关系处理
- 表头生成规范
- 数据展示要求
- 价格计算公式提取规范
- 总分计算规范
- 数据结构要求
- 数据处理规范
- 价格规则处理规范

### 13.4 前端展示规范
- 添加水平滑动条
- 表头自动换行
- 模态框展示

## 14. 技术栈说明
```
后端框架: FastAPI
数据库: SQLAlchemy ORM
PDF处理: PyMuPDF + pdfplumber + PaddleOCR
AI分析: 本地AI模型 (LocalAIAnalyzer)
前端: HTML + CSS + JavaScript (Bootstrap)
文件处理: 支持PDF、Word、Excel导出
并发处理: ProcessPoolExecutor + ThreadPoolExecutor
```

## 15. 关键特性
- **智能文本提取**: 支持PDF直接提取和OCR识别
- **AI智能分析**: 使用本地AI模型进行评分分析
- **价格自动提取**: 从投标文件中自动提取投标总价
- **规则自动提取**: 从招标文件中自动提取评分规则
- **并发处理**: 支持多文件并发分析
- **实时状态**: 提供分析进度实时反馈
- **结果导出**: 支持Excel和Word格式导出
- **跨平台**: 支持Windows/Linux/macOS

## 16. 部署说明
```
1. 安装依赖: pip install -r requirements.txt
2. 启动服务: python main.py
3. 访问地址: http://localhost:8000
4. 上传文件: 通过Web界面上传招标文件和投标文件
5. 开始分析: 确认投标人名称后自动开始分析
6. 查看结果: 在Web界面查看分析结果和导出文件
```

---
*文档生成时间: 2025-09-27*
*项目版本: AI_env2*