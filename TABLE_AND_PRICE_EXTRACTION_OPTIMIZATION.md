# PDF表格识别与投标总价提取优化报告

## 问题描述

1. 当前项目对图形格式PDF文件的文本识别非常不错，但是对表格识别出现混乱，没有体现出表格的逻辑
2. 投标总价的提取出现错误，都没能正确提取出来

## 优化方案

### 1. 表格识别优化

#### 1.1 增强表格结构识别逻辑
- 扩展目标表头关键词，增加与投标相关的关键词：`['评价项目', '评价标准', '投标人', '投标报价', '总价', '总报价']`
- 改进跨页表格合并算法，增强对投标一览表的识别能力
- 优化表格内容结构相似度计算，提高表格合并准确性

#### 1.2 保留表格逻辑结构
- 优化文本清理函数[_clean_text_and_spaces](file:///d:/user/PythonProject/AI_env2/modules/table_analyzer.py#L466-L492)，保留必要的空格以维持可读性
- 改进表格数据转换逻辑，确保表格原有的逻辑结构得以保留

### 2. 投标总价提取优化

#### 2.1 优化投标一览表识别
- 增强价格一览表页面识别算法，提高对"投标一览表"、"开标一览表"、"价格一览表"的识别准确率
- 优化小写和大写价格对照提取逻辑，确保能同时提取阿拉伯数字和中文大写数字

#### 2.2 改进价格提取模式
- 扩展小写价格提取模式，增加更多匹配格式：
  - `(小写).*?￥?\s*([\d,]+\.?\d*)`
  - `(小写金额)[:：\s]*￥?\s*([\d,]+\.?\d*)`
  - `(投标报价)[:：\s]*￥?\s*([\d,]+\.?\d*)`
  - `(总报价)[:：\s]*￥?\s*([\d,]+\.?\d*)`
  - `(总价)[:：\s]*￥?\s*([\d,]+\.?\d*)`
  - `(人民币)[:：\s]*￥?\s*([\d,]+\.?\d*)`
  - `￥\s*([\d,]+\.?\d*)`
  - `([\d,]+\.?\d*)\s*(?:元|人民币)`

- 扩展大写价格提取模式，增加更多匹配格式：
  - `(大写)[:：\s]*([壹贰叁肆伍陆柒捌玖拾佰仟万亿零一二三四五六七八九十百千万亿\s]+)`
  - `(大写金额)[:：\s]*([壹贰叁肆伍陆柒捌玖拾佰仟万亿零一二三四五六七八九十百千万亿\s]+)`
  - `([壹贰叁肆伍陆柒捌玖拾佰仟万亿零一二三四五六七八九十百千万亿\s]+)[:：\s]*(?:元|人民币)`

#### 2.3 增强价格验证机制
- 实现小写和大写价格对照验证机制，提高价格提取准确性
- 增强保证金过滤机制，避免将投标保证金误识别为投标总价
- 优化置信度计算算法，提高对正确价格的识别能力

### 3. 文档更新

#### 3.1 更新项目需求与架构说明文档
- 在"性能优化"部分增加"表格识别优化"和"投标总价提取优化"条目
- 新增"PDF处理优化规范 (v3.14 新增 - 表格与价格提取优化)"章节，详细说明优化内容

## 实施效果

通过以上优化，系统能够：

1. 正确识别并处理跨页表格，保留表格原有的逻辑结构
2. 准确提取投标一览表中的投标总价信息
3. 实现小写和大写价格对照验证，提高价格提取准确性
4. 有效过滤掉投标保证金等干扰项
5. 提高整体价格提取的准确性和可靠性

## 后续建议

1. 持续监控表格识别和价格提取的准确性，收集更多测试案例
2. 考虑增加更多表格和价格相关的识别关键字
3. 进一步优化AI模型在表格处理和价格提取方面的能力
